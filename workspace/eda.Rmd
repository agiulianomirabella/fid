---
title: "eda"
output: html_document

---
```{r}
library(dplyr)
library(ggplot2)
```

```{r}
df <- read.csv("../data/raw/data.csv")
class(df)
str(df)
summary(df)
```

```{r}

ages <- df %>%
  group_by(age, death) %>%
  count() %>%
  mutate(freq = n*100/nrow(filter(df, df$age==age)))

ages

# death_by_age <- df %>% 
#   group_by(age, death) %>% 
#   count() %>%
#   mutate(freq = n*100 / age)
# 
# death_by_age

# death_by_age %>%
#   filter(death==1) %>%
#   ggplot(aes(x=age, y=n)) +
#   geom_point() +
#   theme(axis.text.x = element_text(angle = 25)) +
#   labs(x = "Age", y= "Number of deaths", title="Death Evolution", subtitle= "by age")

# ages <- df %>% 
#   group_by(age, death) %>% 
#   count() %>%
#   mutate(freq = n*100 / nrow(df))


# ages %>%
#   ggplot(aes(x=age, y=n, color=death)) +
#   geom_point() +
#   theme(axis.text.x = element_text(angle = 25)) +
#   labs(x = "x Label", y= "Frequency", title="Death Evolution", subtitle= "By age")

```

